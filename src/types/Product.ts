export interface Product {
  id: number;
  name: string;
  image: string;
  description: string;
  details: string;
  originalPrice: number;
  currentPrice: number;
  installments: number;
  installmentPrice: number;
}
